# 前端 CD — 预留，前端就绪后取消注释
#
# name: CD · Frontend
#
# on:
#   push:
#     branches: [main]
#     paths: ["frontend/**"]
#
# jobs:
#   build-and-push:
#     name: Build & Push
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#
#       - name: Determine version tag
#         id: version
#         run: |
#           echo "tag=dev-${{ github.run_number }}-${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"
#
#       - uses: docker/setup-buildx-action@v3
#
#       - name: Login to registry
#         run: |
#           echo "${{ '{{' }} secrets.REGISTRY_PASSWORD {{ '}}' }}" | \
#             docker login {{ registry_url }} -u ci-bot --password-stdin
#
#       - name: Build and push · web
#         uses: docker/build-push-action@v6
#         with:
#           context: frontend
#           file: frontend/Dockerfile
#           push: true
#           tags: |
#             {{ registry_url }}/{{ org_name }}/{{ project_name }}-web:${{ '{{' }} steps.version.outputs.tag {{ '}}' }}
#             {{ registry_url }}/{{ org_name }}/{{ project_name }}-web:latest
#           cache-from: type=gha,scope=frontend
#           cache-to: type=gha,scope=frontend,mode=max
